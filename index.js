// Write your code here!
// Create a new <h1> element and assign it to a variable `newHeader`
const newHeader = document.createElement("h1");

// Set the id of the new header
newHeader.id = "victory";

// Set the text content to "YOUR-NAME is the champion" (replace YOUR-NAME with your name)
newHeader.textContent = "YOUR-NAME is the champion";

// Append the new header to the body
document.body.append(newHeader);
